[
  {
    "context": "Editor && vim_mode == normal && !menu",
    "bindings": {
      "space 0 0": "repl::Restart"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-`": "workspace::NewCenterTerminal"
    }
  }
]
